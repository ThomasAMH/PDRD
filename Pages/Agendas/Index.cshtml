@page
@model PDRD.Pages.Agendas.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Agenda[0].MeetingDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Agenda[0].SpecialMusicalNumber)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Agenda[0].NonSpeakerProgram)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Agenda) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.MeetingDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SpecialMusicalNumber)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NonSpeakerProgram)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.AgendaID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.AgendaID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.AgendaID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
